include "messages/messages.conf"

db {
  driver = "org.postgresql.Driver"
  url = ${?DATABASE_URL}
}

bot {
  port = 8080
  port = ${?PORT}
  token = ${?BOT_TOKEN}
  url = ${BOT_EXTERNAL_URL}
  username = ${?BOT_USERNAME}
}

timezones = {
  url = "https://timezones.jspade.dev"
  url = ${?TIMEZONES_URL}
}
